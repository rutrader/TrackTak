import { IndustryAveragesResults } from "../src";
import React from "react";

const config = {
  title: "IndustryAveragesResults",
  component: IndustryAveragesResults,
};

const Template = () => {
  return <IndustryAveragesResults />;
};

export const Default = Template.bind({});

export default config;
