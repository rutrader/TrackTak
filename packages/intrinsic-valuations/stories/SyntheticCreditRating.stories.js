import { SyntheticCreditRating } from "../src";
import React from "react";

const config = {
  title: "SyntheticCreditRating",
  component: SyntheticCreditRating,
};

const Template = () => {
  return <SyntheticCreditRating />;
};

export const Default = Template.bind({});

export default config;
