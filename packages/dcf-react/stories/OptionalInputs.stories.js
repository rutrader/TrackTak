import { OptionalInputs } from "../src";
import React from "react";

const config = {
  title: "OptionalInputs",
  component: OptionalInputs,
};

const Template = () => {
  return <OptionalInputs />;
};

export const Default = Template.bind({});

export default config;
