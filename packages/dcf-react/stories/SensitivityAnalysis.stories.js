import { SensitivityAnalysis } from "../src";
import React from "react";

const config = {
  title: "SensitivityAnalysis",
  component: SensitivityAnalysis,
};

const Template = () => {
  return <SensitivityAnalysis />;
};

export const Default = Template.bind({});

export default config;
