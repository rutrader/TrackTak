import { SyntheticRating } from "../src";
import React from "react";

const config = {
  title: "SyntheticRating",
  component: SyntheticRating,
};

const Template = () => {
  return <SyntheticRating />;
};

export const Default = Template.bind({});

export default config;
