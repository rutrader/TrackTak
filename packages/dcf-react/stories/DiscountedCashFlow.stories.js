import { DiscountedCashFlow } from "../src";
import React from "react";

const config = {
  title: "DiscountedCashFlow",
  component: DiscountedCashFlow,
};

const Template = () => {
  return <DiscountedCashFlow />;
};

export const Default = Template.bind({});

export default config;
