import { IndustryAverages } from "../src";
import React from "react";

const config = {
  title: "IndustryAverages",
  component: IndustryAverages,
};

const Template = () => {
  return <IndustryAverages />;
};

export const Default = Template.bind({});

export default config;
