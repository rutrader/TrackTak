import { CostOfCapitalResults } from "../src";
import React from "react";

const config = {
  title: "CostOfCapitalResults",
  component: CostOfCapitalResults,
};

const Template = () => {
  return <CostOfCapitalResults />;
};

export const Default = Template.bind({});

export default config;
